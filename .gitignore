# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data outputs (regenerated by pipeline). The second line matters: the first
# does NOT match one directory deeper, so block outputs were being staged.
data/output/*.csv
data/output/blocks/*.csv

# Real business coordinates (never commit — exact locations are sensitive).
# data/input/ is otherwise tracked, so the GSMM ingest output is named here.
data/input/gsmm_listings.csv

# Extraction manifest: per-indicator config fingerprints driving incremental
# reruns. Derived from the (ignored) output CSVs, so committing it would leave
# a record pointing at files that are not in the repo.
data/output/extraction_manifest.json
data/output/blocks/extraction_manifest.json

# GEE credentials (never commit)
.config/earthengine/

# Environment files
.env
.env.local

# R session leftovers (registry_environment.R is validated with Rscript)
.Rhistory
.RData
.Rproj.user/
